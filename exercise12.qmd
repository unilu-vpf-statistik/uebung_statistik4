---
title: "Übung 12 - Anwendung II: Regression & Vorhersagen"
subtitle: "FS 2026"
author: "Gidon Frischkorn"
format:
  html:
    css: styles.css
    include-after-body: webex.js
filters:
  - webr
tbl-colwidths: [60,40]
---

```{r preparation}
#| include: false
library(exams)
library(exams2forms)
library(here)

exam_files <- list.files(here("questions_uebung12"), pattern = "*.Rmd", full.names = TRUE)
shuffled_questions <- sample(exam_files)
```

## Wiederholung & Vertiefung: Regression & Vorhersagen

In dieser Übung beschäftigen wir uns mit der **linearen Regression** als Verallgemeinerung des Gruppenvergleichs. ANOVA und Regression sind Spezialfälle desselben linearen Modells.

### Das lineare Modell

**y = b₀ + b₁·x + e**

| Koeffizient | Bedeutung |
|---|---|
| **b₀** (Achsenabschnitt) | Vorhergesagter y-Wert bei x = 0 |
| **b₁** (Steigung) | Veränderung in y pro Einheit x |
| **e** (Fehler) | Nicht erklärte Abweichung |

Der **Fehler e** lässt sich geometrisch zeigen: Für jeden Datenpunkt ist das **Residuum** der senkrechte Abstand zwischen dem beobachteten Wert und der Regressionslinie (dem vorhergesagten Wert). Die Regression wählt die Linie so, dass die Summe der quadrierten Residuen minimal wird ("Kleinste-Quadrate-Schätzung"). Das Verständnis dieser Abstände ist zentral für die spätere Modellprüfung in Aufgabe 4.

```{r fig-residuen}
#| echo: false
#| message: false
#| warning: false
#| fig-cap: "Residuen als senkrechte Abstände: Jeder Datenpunkt weicht um einen individuellen Betrag von der Regressionslinie ab — diese Abweichung ist das Residuum."
#| fig-alt: "Scatterplot mit 15 Datenpunkten, einer blauen Regressionslinie und gepunkteten roten senkrechten Linien von jedem Punkt zur Regressionslinie. Die roten Linien visualisieren die Residuen."
#| fig-width: 7
#| fig-height: 4.2
library(ggplot2)

set.seed(7)
n <- 15
x <- seq(1, 10, length.out = n) + rnorm(n, 0, 0.2)
y <- 2 + 0.8 * x + rnorm(n, 0, 1.3)
dat <- data.frame(x = x, y = y)
mod <- lm(y ~ x, data = dat)
dat$y_hat <- predict(mod)

ggplot(dat, aes(x = x, y = y)) +
  geom_smooth(method = "lm", se = FALSE,
              color = "#2c5d8f", linewidth = 0.9) +
  geom_segment(aes(xend = x, yend = y_hat),
               color = "firebrick", linewidth = 0.55, linetype = "dotted") +
  geom_point(size = 2.6, alpha = 0.9) +
  annotate("text", x = 9.7, y = 1.2,
           label = "Residuum =\nbeobachtet − vorhergesagt",
           color = "firebrick", size = 3.4, hjust = 1, vjust = 0,
           lineheight = 0.95) +
  annotate("text", x = 1.3, y = max(dat$y_hat) * 0.95,
           label = "Regressionslinie\n(ŷ = b₀ + b₁·x)",
           color = "#2c5d8f", size = 3.4, hjust = 0, vjust = 1,
           lineheight = 0.95) +
  labs(x = "Prädiktor (x)", y = "Zielvariable (y)") +
  theme_minimal(base_size = 11)
```

### Korrelation ≠ Kausalität

Regression zeigt **Zusammenhang**, nicht **Ursache-Wirkung**. Ein signifikanter Koeffizient bedeutet nicht, dass x die Veränderung in y verursacht.

### Modellgüte: R²

- R² = Anteil der Varianz in y, der durch das Modell erklärt wird
- In der Psychologie oft R² = .05–.30 (normal)
- **R² steigt mit jedem Prädiktor** → adjustiertes R² oder Modellvergleich verwenden

### Interaktion (Moderation)

Eine **Interaktion** bedeutet: Der Effekt von x₁ auf y ist unterschiedlich je nach x₂. Grafisch: **nicht-parallele** Regressionslinien.

### Grenzen

- **Extrapolation**: Vorhersage ausserhalb des Datenbereichs ist unzuverlässig
- **Overfitting**: Zu viele Prädiktoren bei wenig Daten → Modell passt auf Stichprobe, versagt bei neuen Daten

## Übungen

Beantworten Sie die folgenden Fragen zu Regression und Vorhersagen.

```{r print_questions}
#| echo: false
#| message: false
#| results: "asis"
exams2forms(shuffled_questions, mathjax = TRUE)
```

```{webr-r}
# Herunterladen des Datensatzes
base_url <- "https://unilu-vpf-statistik.github.io/uebung_statistik4/daten/"
download.file(paste0(base_url, "WM_Fragebögen_NEU.csv"), "WM_Fragebögen_NEU.csv")

# Laden der benötigten Pakete
library(dplyr)
library(ggplot2)

# Daten einlesen
# Pre/Post-Datensatz: _t1 = Baseline, _t2 = nach Intervention.
wm_daten <- read.csv2("WM_Fragebögen_NEU.csv")

# --- Aufgabe 1: Einfache Regression ---
# Wie gut sagt der Schlaf nach der Intervention den WM-Score nach
# der Intervention vorher?
modell1 <- lm(wm_score_t2 ~ sleep_hours_t2, data = wm_daten)
summary(modell1)
# Wie interpretieren Sie den Koeffizienten für sleep_hours_t2?

# --- Aufgabe 2: Scatterplot mit Regressionslinie ---
ggplot(wm_daten, aes(x = sleep_hours_t2, y = wm_score_t2)) +
  geom_point(alpha = 0.5) +
  geom_smooth(method = "lm", se = TRUE) +
  labs(x = "Schlafdauer nach Intervention (h)",
       y = "WM-Score nach Intervention") +
  theme_minimal()

# --- Aufgabe 3: Multiple Regression mit Baseline-Kontrolle ---
# Wenn wir den WM-Score VOR der Intervention (wm_score_t1) als
# zusätzlichen Prädiktor aufnehmen, kontrollieren wir für die
# individuelle WM-Kapazität. Wie verändert sich der Schlaf-Effekt?
modell2 <- lm(wm_score_t2 ~ sleep_hours_t2 + wm_score_t1, data = wm_daten)
summary(modell2)

# --- Aufgabe 4: Residualdiagnostik ---
plot(modell2)
# Was sagen die Diagnostik-Plots über die Modellannahmen?
```

------------------------------------------------------------------------

[![](images/Logo_UniLu.png){fig-align="center" width="300"}](https://www.unilu.ch/fakultaeten/vpf/uebersicht/)
