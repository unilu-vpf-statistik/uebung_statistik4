---
title: "Übung 7 - Deskriptive Statistik berechnen"
subtitle: "FS 2026"
author: "Gidon Frischkorn"
format:
  html:
    css: styles.css
    include-after-body: webex.js
filters:
  - webr
tbl-colwidths: [60,40]
---

```{r preparation}
#| include: false
library(exams)
library(exams2forms)
library(here)

exam_files <- list.files(here("questions_uebung7"), pattern = "*.Rmd", full.names = TRUE)
shuffled_questions <- sample(exam_files)
```

## Wiederholung & Vertiefung: Deskriptive Statistik

In dieser Übung beschäftigen wir uns mit der Berechnung und Interpretation **deskriptiver Kennwerte** in R. Deskriptive Statistik ist kein Selbstzweck — sie ist die **Entscheidungsgrundlage** für weiterführende Analysen.

### Kennwerte im Überblick (Auffrischung)

| Skalenniveau | Lagemass | Streuungsmass |
|---|---|---|
| **Nominal** | Modus | Häufigkeiten |
| **Ordinal** | Median | IQR |
| **Metrisch (symmetrisch)** | Mittelwert | SD |
| **Metrisch (schief)** | Median | IQR |

**Faustregel**: Wenn Mittelwert ≠ Median, deutet das auf eine schiefe Verteilung hin. In diesem Fall ist der Median das robustere Lagemass.

### Was berechnet R eigentlich?

R trifft bei deskriptiven Funktionen bestimmte **Standardeinstellungen**, die man kennen sollte:

- **`sd()` rechnet mit N−1**: R berechnet die *Stichprobenschätzung* der Standardabweichung (Division durch N−1), nicht den deskriptiven Wert (Division durch N). Das ist die richtige Wahl für inferentielle Analysen — keine manuelle Korrektur nötig. Gleiches gilt für `var()`.

- **NA-Propagation**: `mean(x)` gibt `NA` zurück, sobald *ein einziger* fehlender Wert vorliegt. R zwingt uns damit zu einer bewussten Entscheidung: Mit `na.rm = TRUE` schliessen wir fehlende Werte explizit aus. Das gilt für `mean()`, `sd()`, `median()`, `var()`, `min()`, `max()` und `sum()`.

- **Immer N berichten**: Jede deskriptive Statistik sollte von der Anzahl gültiger Werte begleitet werden. `n()` zählt alle Zeilen (inkl. NA), `sum(!is.na(x))` zählt nur gültige Werte. Ohne N fehlt die Grundlage zur Einordnung.

### Split-Apply-Combine

Für gruppierte Auswertungen folgen wir dem **Split-Apply-Combine**-Prinzip:

1. **Split**: Daten nach Gruppen aufteilen (`group_by()`)
2. **Apply**: Kennwert pro Gruppe berechnen (`summarise()`)
3. **Combine**: Ergebnisse zusammenführen (passiert automatisch)

## Übungen

Beantworten Sie die folgenden Fragen zur deskriptiven Statistik.

```{r print_questions}
#| echo: false
#| message: false
#| results: "asis"
exams2forms(shuffled_questions, mathjax = TRUE)
```

```{webr-r}
# Herunterladen der Datensätze
base_url <- "https://unilu-vpf-statistik.github.io/uebung_statistik4/daten/"
download.file(paste0(base_url, "WM_Fragebögen_NEU.csv"), "WM_Fragebögen_NEU.csv")
download.file(paste0(base_url, "Lab_Kodierung_NEU.dat"), "Lab_Kodierung_NEU.dat")

# Laden der benötigten Pakete
library(dplyr)
library(tidyr)
library(stringr)

# Daten einlesen und zusammenführen (wie in Übung 4 & 5 erarbeitet)
# Hinweis: Ab dieser Übung verwenden wir die aktualisierten Datensätze
# mit Pre/Post-Messung (Suffix _NEU). Die Zielvariablen sind nun jeweils
# zweimal erhoben: vor der Intervention (_t1) und nach der Intervention (_t2).
wm_daten <- read.csv2("WM_Fragebögen_NEU.csv")
lab_daten <- read.table("Lab_Kodierung_NEU.dat", header = TRUE, sep = "\t")

lab_daten <- lab_daten |>
  mutate(
    id = id_raw |>
      trimws() |>
      str_remove("ID-") |>
      str_remove("P") |>
      as.integer(),
    gruppe = case_when(
      group_raw %in% c("Kontrollgruppe", "Kontrolle",
                       "CON", "control", "ctrl") ~ "KG",
      group_raw == "Stress"                      ~ "Stress",
      .default = NA_character_
    )
  )

wm_daten <- wm_daten |>
  left_join(lab_daten |> select(id, gruppe), by = "id")
```

**Hinweis:** In der Vorlesung haben wir den Code am Beispiel `wm_score_t1` × `gruppe` gesehen. In den folgenden Aufgaben wenden Sie das **gleiche Muster** auf **andere Variablen** an — Sie müssen also nicht kopieren, sondern übertragen. Ein kurzer Blick in die Vorlesungsfolien als Referenz ist völlig in Ordnung.

```{webr-r}
# --- Aufgabe 1: Dimension GRUPPEN (~5 Min) ---
# Forschungsfrage: Unterscheiden sich Frauen und Männer in dieser
# Stichprobe in ihrer durchschnittlichen Schlafdauer zur Baseline
# (sleep_hours_t1)?
#
# Erstellen Sie eine deskriptive Tabelle mit N, M, SD und Mdn für
# sleep_hours_t1, getrennt nach gender.
#
# Tipp: Das Muster ist identisch zu dem aus der Vorlesung — nur die
# Gruppierungsvariable und die Zielvariable ändern sich.

# Ihr Code:


# Interpretation:
# - Wie gross ist der Unterschied zwischen den Gender-Gruppen?
# - Sehen Sie Hinweise auf eine schiefe Verteilung (M vs. Mdn)?
```

```{webr-r}
# --- Aufgabe 2: Dimension VARIABLEN (~8 Min) ---
# Forschungsfrage: Wie sieht die Stichprobe insgesamt aus? Erstellen
# Sie eine Reporting-Tabelle mit N, M, SD und Mdn für ALLE numerischen
# Variablen — eine Zeile pro Variable.
#
# Tipp: Nutzen Sie den Long-first-Ansatz aus der Vorlesung
# (pivot_longer → group_by(variable) → summarise).
#
# Achtung: `id` ist zwar numerisch, aber keine inhaltliche Variable.
# Was passiert, wenn Sie sie im Pivot drinlassen?

# Ihr Code:


# Zum Nachdenken:
# - Welche Variable hat die grösste Streuung (SD)? Warum?
# - Bei welcher Variable ist M ≠ Mdn — und was heisst das?
```

```{webr-r}
# --- Aufgabe 3: Beide Dimensionen kombiniert (~8 Min) ---
# Forschungsfrage: Sind die experimentellen Bedingungen (gruppe) VOR
# der Intervention sowohl zwischen den Gruppen als auch zwischen den
# Geschlechtern vergleichbar? Unterschiede in sleep_hours_t1,
# stress_rating_t1 oder age wären ein Problem für die Interpretation
# des wm_score_t2-Vergleichs nach der Intervention.
#
# Erstellen Sie eine Tabelle mit N, M und SD für alle Baseline-Variablen
# (alle _t1 Variablen + age), getrennt nach gruppe UND gender.
# Eine Zeile pro (gruppe × gender × variable).
#
# Tipp: group_by() akzeptiert mehrere Variablen gleichzeitig.

# Ihr Code:


# Interpretation:
# - Bei welchen Variablen zeigen sich Gruppen- oder Gender-Unterschiede?
# - Sind die Zellen (gruppe × gender) vergleichbar besetzt?
# - Wären diese Unterschiede problematisch für den Vergleich des
#   wm_score_t2 zwischen KG und Stress?
```

```{webr-r}
# --- Bonus: Häufigkeiten für kategoriale Variablen ---
# Wer früh fertig ist: Sind die Geschlechter gleichmässig über die
# experimentellen Bedingungen verteilt?
#
# Erstellen Sie eine Kreuztabelle von gender × gruppe mit count().

# Ihr Code:

```

------------------------------------------------------------------------

[![](images/Logo_UniLu.png){fig-align="center" width="300"}](https://www.unilu.ch/fakultaeten/vpf/uebersicht/)
