Question
========
Bevor Sie eine ANOVA rechnen, prüfen Sie die Modellannahmen. Sie führen folgenden Code aus:

```r
# Schritt 1: Bartlett-Test (Varianzhomogenität)
bartlett.test(wm_score ~ gruppe, data = daten)
# Bartlett test of homogeneity of variances
# Bartlett's K-squared = 5.62, df = 1, p-value = 0.018

# Schritt 2: QQ-Plot der Residuen
modell <- aov(wm_score ~ gruppe, data = daten)
plot(modell, which = 2)
# (QQ-Plot zeigt Punkte nahe an der Diagonale)
```

Was ist der geeignetste nächste Schritt?

Answerlist
----------
* Die ANOVA kann ohne Anpassung durchgeführt werden, weil der QQ-Plot unauffällig ist.
* Der Bartlett-Test ist signifikant (p = .018), daher sind die Varianzen in den Gruppen ungleich. Eine **Welch-ANOVA** (`oneway.test()`) ist angemessener, da sie keine gleichen Varianzen voraussetzt.
* Die Daten müssen transformiert werden (z. B. logarithmieren), bevor eine Analyse durchgeführt werden kann.
* Der signifikante Bartlett-Test bedeutet, dass die Normalverteilungsannahme verletzt ist und ein Kruskal-Wallis-Test verwendet werden muss.

Solution
========
Der Bartlett-Test prüft die Varianzhomogenität — ein signifikantes Ergebnis (p = .018) zeigt, dass die Streuung in den Gruppen unterschiedlich ist. Die Lösung ist eine Welch-ANOVA (`oneway.test(wm_score ~ gruppe, data = daten)`), die keine gleichen Varianzen voraussetzt. Der QQ-Plot zeigt, dass die Normalverteilung der Residuen unauffällig ist — deshalb ist ein Kruskal-Wallis-Test nicht nötig. Eine Datentransformation wäre ein unverhältnismässig starker Eingriff, wenn das einzige Problem unterschiedliche Varianzen sind. Die Antwort, dass die ANOVA ohne Anpassung durchgeführt werden könne, ignoriert das Problem der ungleichen Varianzen, obwohl der QQ-Plot zwar unauffällig ist.

Meta-information
================
exname: annahmen_workflow
extype: schoice
exsolution: 0100
exshuffle: FALSE
